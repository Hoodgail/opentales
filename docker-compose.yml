services:
  backend:
    build:
      context: .
      dockerfile: packages/backend/Dockerfile
    container_name: opentales-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: ${DATABASE_URL:-postgresql://opentales:opentales@postgres:5432/opentales?schema=public}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://opentales.hoodgail.me}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-https://opentales-api.hoodgail.me}
      MCP_ALLOWED_ORIGINS: ${MCP_ALLOWED_ORIGINS:-https://opentales.hoodgail.me,https://tale.yasui.io}
      MCP_PUBLIC_URL: ${MCP_PUBLIC_URL:-https://opentales.hoodgail.me/mcp}
      MCP_OAUTH_ISSUER: ${MCP_OAUTH_ISSUER:-https://opentales.hoodgail.me}
    expose:
      - "4000"
    networks:
      - default
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.opentales-api.rule=Host(`opentales-api.lumina.pw`)"
      - "traefik.http.routers.opentales-api.entrypoints=web"
      - "traefik.http.services.opentales-api.loadbalancer.server.port=4000"

  frontend:
    build:
      context: .
      dockerfile: packages/frontend/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://opentales-api.hoodgail.me}
    container_name: opentales-frontend
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.opentales.rule=Host(`opentales.lumina.pw`)"
      - "traefik.http.routers.opentales.entrypoints=web"
      - "traefik.http.services.opentales.loadbalancer.server.port=80"

networks:
  dokploy-network:
    external: true
